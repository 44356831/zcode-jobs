# 任务：zcode-relay 全链路连通测试（第二轮）

name: e2e-test-20260921-1339
description: 飞书通道恢复后验证完整链路：Git 投递 -> 桥接服务发现 -> 飞书通知 -> 小糯执行 -> 报告落盘 -> 自动回传。任务本身极轻，10 秒内可完成。

## 执行规范

1. 只读操作 + 写 /tmp 标记文件，不碰其他任何文件
2. 全程不读取、不回传任何密钥/token/配置值
3. 完成后把完整报告写入 `/opt/zcode-relay/repo/results/e2e-test-20260921-1339/report.md`

## 步骤

### step 1：确认触发通道
请回答：你是否在飞书群里收到了桥接服务的任务通知（消息含「【zcode-relay】发现新任务」字样）？在报告里写明收到/未收到、大致时间。

### step 2：记录执行环境
```bash
hostname && date '+%F %T' && echo "e2e round2 - $(date '+%F %T')" > /tmp/zcode_e2e_marker2.txt && cat /tmp/zcode_e2e_marker2.txt
```

### step 3：确认桥接服务存活
```bash
systemctl is-active zcode-bridge
```

## 输出要求

- 逐步给出命令与输出
- 最后一节「结论」：全链路各环节是否成立（Git 取件 ✅/❌、飞书通知 ✅/❌、执行 ✅/❌、报告落盘 ✅/❌），给主人一句话总结
