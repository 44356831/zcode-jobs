# 任务：<任务标题>

name: <任务名-带时间戳，如 mytask-20260921-1250>
description: <一句话说明这个工作流要做什么>

## 执行规范（每个任务都必须保留本节，relay 依赖它回收结果）

1. 按下方步骤逐条执行，每步记录命令与输出；只读/安全操作优先，涉及修改
   或删除的动作必须先停下来在报告里向主人确认
2. 全部步骤结束后，把完整报告**写入文件**：
   `/opt/zcode-relay/repo/results/<本任务name>/report.md`
   （目录不存在就创建；report.md 是 relay 回收结果的唯一信号）
3. 严禁在报告或任何输出中回传密钥、token、配置文件的值——只允许出现变量名
4. 执行受阻时也要写 report.md，把阻塞原因、已排查的路径写清楚，状态写 blocked

## 步骤

### step 1：<做什么>
```bash
<命令>
```

### step 2：<做什么>
```bash
<命令>
```

## 输出要求

- 逐步给出命令与输出
- 最后一节「结论」：达成/未达成、关键数据、给主人的下一步建议
